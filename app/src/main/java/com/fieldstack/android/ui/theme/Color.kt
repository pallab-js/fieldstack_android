package com.fieldstack.android.ui.theme

import androidx.compose.ui.graphics.Color

// ── BudgetZen Palette ──────────────────────────────────────────────────────
val Mint        = Color(0xFF10B981)   // Primary — CTAs, progress, success
val MintDark    = Color(0xFF059669)   // Primary hover / pressed
val MintLight   = Color(0xFF10B9811A) // Primary tint (10% alpha)

val Sky         = Color(0xFF38BDF8)   // Secondary — links, tips, info
val SkyLight    = Color(0xFF38BDF81A) // Secondary tint

val WarmGray    = Color(0xFFA8A29E)   // Tertiary — muted, inactive
val Stone       = Color(0xFF78716C)   // Neutral — secondary text
val StoneLight  = Color(0xFFF5F5F4)   // Hover backgrounds
val StoneDark   = Color(0xFF57534E)   // Label text
val StoneDeep   = Color(0xFF1C1917)   // Primary text / shadow base

val AppBackground = Color(0xFFFAFFFE) // Root canvas
val Surface       = Color(0xFFFFFFFF) // Cards, panels, modals
val SurfaceBorder = Color(0xFFE7E5E4) // Default card border
val InputBorder   = Color(0xFFD6D3D1) // Default input border

val Success = Color(0xFF10B981)
val Warning = Color(0xFFF59E0B)
val WarningLight = Color(0xFFF59E0B1A)
val WarningDark  = Color(0xFFD97706)
val Error   = Color(0xFFEF4444)
val ErrorLight   = Color(0xFFEF44441A)
val ErrorDark    = Color(0xFFDC2626)
val Info    = Color(0xFF38BDF8)
